/** Style: إدارة ITL الذهبية — محرر منظم يقسم المحتوى العام إلى مجموعات واضحة قابلة للحفظ. */
import { UiManager } from '@/components/itl/admin/ui-manager'

export const dynamic = 'force-dynamic'

export default function AdminUiPage() {
  return <UiManager />
}
