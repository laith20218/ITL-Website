/** Style: مسار الإنجاز الذهبي — صفحة حساب مرتبطة بالواجهة العامة وتكشف الإدارة بحسب الدور. */
import { AccountHub } from '@/components/itl/account-hub'

export default function AccountPage() {
  return <AccountHub />
}
