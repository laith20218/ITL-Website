# 🔧 دليل إصلاحات بيئة Windows لمشروع ITL

بناءً على التجربة الفعلية لتشغيل المشروع على Windows 10، إليك الحلول لكل المشاكل التي قد تواجهك.

---

## ⚡ الحل السريع (ملف واحد يصلح كل شيء)

1. انسخ ملف `start.bat` إلى مجلد المشروع الرئيسي (بجانب `package.json`)
2. انسخ ملف `package.json` المُعدّل (يستبدل الموجود)
3. انسخ ملف `.gitignore` المُحدّث
4. **اضغط مرتين على `start.bat`** — سيقوم بكل شيء تلقائيًا:
   - تثبيت الاعتمادات (إن لم تكن مثبتة)
   - إنشاء ملف `.env` (إن لم يكن موجودًا)
   - إنشاء قاعدة البيانات + البيانات الأولية
   - حذف ملف القفل العالق
   - قتل أي عملية على المنفذ 3000
   - تشغيل الخادم

---

## 🛠️ المشاكل والحلول التفصيلية

### المشكلة 1: `bun: command not found: tee`

**السبب:** سكربت `dev` في `package.json` يستخدم `tee` (أمر Linux) لتسجيل الـ log في ملف، وهذا الأمر غير موجود في Windows.

**الحل:** استخدم الـ `package.json` المُعدّل في هذا المجلد. التغيير:

```json
// قبل (Linux فقط):
"dev": "next dev -p 3000 2>&1 | tee dev.log"

// بعد (متوافق مع Windows + Linux):
"dev": "next dev -p 3000"
```

**البديل اليدوي:** شغّل مباشرة بدون `bun run`:
```cmd
bunx next dev -p 3000
```

---

### المشكلة 2: `EADDRINUSE: address already in use :::3000`

**السبب:** المنفذ 3000 مشغول بعملية سابقة (ربما خادم تعطل ولم يُغلق properly).

**الحل 1 (سريع):** استخدم منفذًا آخر:
```cmd
bunx next dev -p 3001
```

**الحل 2 (قتل العملية):**
```cmd
REM ابحث عن PID العملية على المنفذ 3000
netstat -aon | findstr ":3000"

REM ستجد شيئًا مثل: TCP 0.0.0.0:3000  0.0.0.0:0  LISTENING  12345
REM الرقم الأخير (12345) هو PID

REM اقتل العملية
taskkill /f /pid 12345
```

**الحل 3 (سكربت آلي في start.bat):**
ملف `start.bat` يقوم بهذا تلقائيًا قبل بدء التشغيل.

---

### المشكلة 3: `Unable to acquire lock at .next\dev\lock`

**السبب:** ملف قفل (lock file) عالق من محاولة إقاف غير نظيفة للخادم.

**الحل:**
```cmd
del .next\dev\lock
```

أو احذف مجلد `.next` كاملًا (سيُعاد إنشاؤه تلقائيًا):
```cmd
rmdir /s /q .next
```

> 💡 ملف `start.bat` يحذف هذا الملف تلقائيًا قبل كل تشغيل.

---

### المشكلة 4: `'bun' is not recognized as an internal or external command`

**السبب:** بعد تثبيت Bun، متغير الـ PATH لم يُحدّث في الطرفية الحالية.

**الحل:** أغلق نافذة CMD/PowerShell وافتح واحدة جديدة. أو نفّذ:
```cmd
REM في CMD:
refreshenv

REM أو أعد تشغيل الكمبيوتر
```

---

### المشكلة 5: `Author identity unknown` عند git commit

**السبب:** Git يحتاج معرفة هويتك قبل أي commit.

**الحل:**
```cmd
git config --global user.email "your-email@example.com"
git config --global user.name "Your Name"
```

---

### المشكلة 6: التشغيل من مجلد خاطئ (`C:\Windows\system32`)

**السبب:** فتحت CMD كـ Administrator فبدأت في `system32`.

**الحل:** انتقل لمجلد المشروع أولًا:
```cmd
cd C:\Users\YourName\itl-website
```

> 💡 **نصيحة:** افتح CMD في مجلد المشروع مباشرة: اذهب للمجلد في File Explorer → اكتب `cmd` في شريط العنوان → Enter.

---

## 📋 أوامر Windows المفيدة

### التشغيل السريع (بعد الإعداد الأول)
```cmd
REM في مجلد المشروع:
start.bat
```

### أوامر يدوية بديلة
```cmd
REM تثبيت الاعتمادات
bun install

REM إعداد قاعدة البيانات
bunx prisma db push
bunx tsx scripts/seed.ts

REM التشغيل (منفذ 3000)
bunx next dev -p 3000

REM التشغيل (منفذ بديل 3001)
bunx next dev -p 3001

REM بناء للإنتاج
bunx next build

REM تشغيل نسخة الإنتاج
bunx next start
```

---

## 🔑 بيانات الدخول

| الصفحة | الرابط |
|--------|--------|
| الموقع | `http://localhost:3000` |
| لوحة التحكم | `http://localhost:3000/admin-login` |
| البريد | `demo@itl.com` |
| كلمة المرور | `demo1234` |

---

## 📦 محتويات هذا المجلد

| الملف | الوصف |
|------|------|
| `start.bat` | سكربت تشغيل شامل لـ Windows — يصلح كل شيء تلقائيًا |
| `package.json` | نسخة معدّلة بدون `tee` (متوافقة مع Windows) |
| `.gitignore` | ملف محدّث يتجاهل ملفات القفل وقاعدة البيانات والـ builds |
| `WINDOWS-FIXES-GUIDE.md` | هذا الدليل |

---

## ✅ الخطوات الموصى بها

1. **انسخ الملفات الثلاثة** (`start.bat`, `package.json`, `.gitignore`) إلى مجلد المشروع
2. **استبدل** `package.json` و `.gitignore` الموجودين
3. **شغّل `start.bat`** بالضغط المزدوج
4. **انتظر** — سيقوم بكل شيء وسيفتح الخادم
5. **افتح المتصفح** على `http://localhost:3000`

---

## 🚀 الخطوة التالية: النشر على الإنترنت

بعد التأكد من عمل الموقع محليًا، اتبع دليل النشر PDF (`ITL-الدليل-الشامل-للنشر-والإدارة.pdf`) لنشر الموقع على Vercel مجانًا.

**تذكر:** غيّر كلمة مرور المشرف من `demo1234` قبل النشر!

---

**ITL — Idea To Life © 2026**
