# ITL — Idea To Life | مشروع الموقع الكامل

**الإصدار:** 2.0 — يونيو 2026
**التقنيات:** Next.js 16 + TypeScript + Prisma + Tailwind CSS 4 + shadcn/ui

---

## 🚀 التشغيل السريع

```bash
# 1. تثبيت الاعتمادات
bun install

# 2. إعداد قاعدة البيانات
bunx prisma db push

# 3. تعبئة البيانات الأولية (11 خدمة + 12 مقالة + مستخدم مشرف)
bun run scripts/seed.ts

# 4. تشغيل الموقع
bun run dev
# أو: bunx next dev -p 3000
```

**الموقع:** http://localhost:3000
**لوحة التحكم:** http://localhost:3000/admin-login
**البريد:** demo@itl.com
**كلمة المرور:** demo1234

---

## 📋 كل المميزات

### واجهة المستخدم (11 خدمة)
1. البحث العلمي والرسائل الجامعية
2. الترجمة الاحترافية
3. التصميم الجرافيكي والهويات البصرية
4. الإنتاج السمعي والبصري
5. التسويق الرقمي والمواقع
6. الطباعة والمنتجات الورقية
7. تصميم وتطوير المواقع الإلكترونية
8. تطوير تطبيقات أندرويد
9. تطوير تطبيقات iOS
10. تطوير بوتات تلغرام
11. قوائم منتجات رقمية للمتاجر والمطاعم

### لوحة تحكم المشرف (8 أقسام)
- 📊 Dashboard مع إحصائيات + زيارات
- 🛠️ إدارة الخدمات (CRUD)
- 📂 معرض الأعمال (رفع صور/فيديو/PDF)
- 📝 إدارة المقالات (CRUD)
- 📨 صندوق الرسائل (فلترة + رد)
- 👥 إدارة المستخدمين (تعديل + حذف + إعادة تعيين كلمة مرور)
- 📈 تحليلات الزيارات (رسوم بيانية + أجهزة + مصادر)
- ⚙️ إعدادات الموقع (5 تبويبات: Hero, About, Contact, Social+QR, SEO)

### مميزات إضافية
- 🌙 وضع ليلي/نهاري
- 📱 تصميم متجاوب 100%
- 🔐 نظام مصادقة JWT كامل
- 💰 دفع شام كاش (اختياري في نموذج التواصل)
- 📱 تيلغرام + مجتمع واتساب + باركودات QR
- ✨ تأثيرات نيون وحركات احترافية
- 🔍 بحث في المقالات
- 📊 تتبع الزيارات تلقائيًا

### معلومات التواصل
- **البريد:** ITL.Team.2023@gmail.com
- **الهاتف/واتساب:** +963 981 581 384
- **العنوان:** حمص، سوريا
- **ساعات العمل:** الأحد - الخميس، 9 صباحًا - 4 عصرًا

---

## 📂 بنية المشروع

```
itl-website/
├── src/
│   ├── app/                    # صفحات Next.js
│   │   ├── layout.tsx          # Root layout (RTL + fonts + theme)
│   │   ├── page.tsx            # الصفحة الرئيسية
│   │   ├── globals.css         # نظام التصميم الفاخر + neon effects
│   │   ├── admin-login/        # صفحة دخول المشرف
│   │   ├── admin/              # لوحة التحكم (8 أقسام)
│   │   ├── services/[slug]/    # صفحة تفاصيل خدمة
│   │   ├── portfolio/          # معرض الأعمال
│   │   └── api/                # 25+ API routes
│   ├── components/
│   │   ├── itl/                # مكونات ITL المخصصة
│   │   │   ├── admin/          # مكونات لوحة التحكم
│   │   │   └── *.tsx           # مكونات الموقع
│   │   └── ui/                 # مكونات shadcn/ui
│   ├── hooks/                  # React hooks
│   └── lib/                    # auth, db, admin-guard, utils
├── prisma/schema.prisma        # 6 نماذج قاعدة بيانات
├── scripts/seed.ts             # بيانات أولية (11 خدمة + 12 مقالة)
├── public/                     # ملفات ثابتة
├── .env                        # متغيرات البيئة
├── .env.example
├── .gitignore
└── package.json
```

---

## ⚠️ ملاحظات مهمة

1. **غيّر كلمة مرور المشرف** قبل النشر على الإنتاج
2. **أنشئ AUTH_SECRET قوي**: `openssl rand -base64 32`
3. للنشر على Vercel: استبدل SQLite بـ PostgreSQL (Neon مجاني)
4. ملف `.env` لا يُرفع لـ GitHub (موجود في .gitignore)

---

**ITL — Idea To Life © 2026**
